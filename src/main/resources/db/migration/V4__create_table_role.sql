CREATE TABLE SIFY_SCHEMA.ROLE
(
    id        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    role_name VARCHAR(255),
    users_id  BIGINT,
    CONSTRAINT pk_role PRIMARY KEY (id)
);

ALTER TABLE sify_schema.ROLE
    ADD CONSTRAINT FK_ROLE_ON_USERS FOREIGN KEY (users_id) REFERENCES sify_schema.users (id);